apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vectaris.fullname" . }}-config
  labels:
    {{- include "vectaris.labels" . | nindent 4 }}
data:
  # Add non-sensitive configuration here if needed
